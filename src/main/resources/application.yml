spring:
  output:
    ansi:
      enabled: always
  application:
    name: e-commerce
  datasource:
    url: jdbc:mysql://localhost:3306/commerce
    username: scope
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
